# ARCHIVE: Single-FGT baseline config from earlier topology
# ==========================================================
# This config was backed up from a single FortiGate VM (v7.4.12 eval)
# during the original Day 04 lab work (July 2026).
#
# IT IS OBSOLETE — the current architecture uses dual-FGT HA:
#   - FGT-Primary:  port2=192.168.10.1/24 (LAN1), port3=169.254.0.1/30 (HA)
#   - FGT-Secondary: port2=192.168.20.1/24 (LAN2), port3=169.254.0.2/30 (HA)
#
# This archive is retained as a reference for baseline syntax only.
# Do NOT restore this config onto current FGT nodes — IPs and
# interface assignments are incompatible with current topology.
#
# Key diffs from current design:
#   - port2 IP was 10.0.1.1/24 (should be 192.168.10.1/24)
#   - port3 was unconfigured (should be 169.254.0.x/30 for HA)
#   - port4 does not exist under eval license (ports 1-3 only)
#   - Was single FGT, no HA cluster config
#
# Archived: 2026-07-20
